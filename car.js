const Car = class Car {
constructor ( make,
              model,
              carStatus,
              color,
              trimLevels,
              yearBuilt
              ){
                this.make=make;
                this.model= model;
                this.carStatus= carStatus;
                this.color = color;
                this.trimLevels= trimLevels; 
                this.yearBuilt =yearBuilt;
              }
}